-
  name: Destroy container
  hosts: localhost
  gather_facts: false

  tasks:
    -
      name: Kill Containers
      containers.podman.podman_container:
        name: "{{ item }}"
        state: absent
        timeout: 5
      loop: "{{ groups['molecule'] }}"
